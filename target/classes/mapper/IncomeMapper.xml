<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.billmanager.jizhang.mapper.IncomeMapper">

    <resultMap id="BaseResultMap" type="com.billmanager.jizhang.entity.Income">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="category_id" property="categoryId"/>
        <result column="amount" property="amount"/>
        <result column="transaction_date" property="transactionDate"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.billmanager.jizhang.entity.Income" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO income (user_id, category_id, amount, transaction_date, description)
        VALUES (#{userId}, #{categoryId}, #{amount}, #{transactionDate}, #{description})
    </insert>

    <update id="update" parameterType="com.billmanager.jizhang.entity.Income">
        UPDATE income
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="transactionDate != null">transaction_date = #{transactionDate},</if>
            <if test="description != null">description = #{description},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM income WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM income WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM income WHERE user_id = #{userId} ORDER BY transaction_date DESC
    </select>

    <select id="findByUserIdAndDateRange" resultMap="BaseResultMap">
        SELECT * FROM income 
        WHERE user_id = #{userId} 
        AND transaction_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY transaction_date DESC
    </select>

    <select id="findByUserIdAndCategoryId" resultMap="BaseResultMap">
        SELECT * FROM income 
        WHERE user_id = #{userId} 
        AND category_id = #{categoryId}
        ORDER BY transaction_date DESC
    </select>

    <select id="findByUserIdOrderByDateDesc" resultMap="BaseResultMap">
        SELECT * FROM income 
        WHERE user_id = #{userId} 
        ORDER BY transaction_date DESC
    </select>

</mapper>
